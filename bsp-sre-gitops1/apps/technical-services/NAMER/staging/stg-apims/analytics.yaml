apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apims-analytics-onboarding
  namespace: stg-apims
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: stg-apims
      interval: 5m
  # Values overridden for this deployment
  values:
    namespace: stg-apims
    releaseName: analytics-jv1m51r
    applicationName: analytics
    gitRepo:
      repoName: apims-analytics-config
      ref:
        branch: namer-stg
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp  
    helmRelease:
      valuesFiles:
        - helm/analytics/values.yaml
        - helm/analytics/overrides/namer-stg.yaml
    alert:
      channel: apims-analytics-gitops