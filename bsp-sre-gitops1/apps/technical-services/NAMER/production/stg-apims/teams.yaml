apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apims-teams-onboarding
  namespace: stg-apims
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: stg-apims
      interval: 5m
  # Values overridden for this deployment
  values:
    namespace: stg-apims
    releaseName: teams-v3th9mj
    applicationName: apims-teams
    gitRepo:
      repoName: apims-teams-config
      ref:
        branch: namer-stg
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/apims-teams/values.yaml
        - helm/apims-teams/overrides/namer/us-stg-override.yaml
    alert:
      channel: apims-teams-gitops
