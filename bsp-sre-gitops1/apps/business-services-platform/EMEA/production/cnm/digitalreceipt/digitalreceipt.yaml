apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: digitalreceipt-on-boarding
  namespace: cnm
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: cnm
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: cnm
    releaseName: digitalreceipt
    applicationName: digitalreceipt
    gitRepo:
      repoName: digital-receipt-config
      ref:
        branch: emea-prd
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/digitalreceipt/values.yaml
        - helm/digitalreceipt/overrides/values-emea-prod.yaml
    alert:
      channel: bsp-emea-prd-flux-applications
