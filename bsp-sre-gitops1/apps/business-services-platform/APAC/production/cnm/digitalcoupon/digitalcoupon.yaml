apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: digitalcoupon-on-boarding
  namespace: cnm
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: cnm
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: cnm
    releaseName: digitalcoupon
    applicationName: digitalcoupon
    gitRepo:
      repoName: digital-coupon-config
      ref:
        branch: apac-prd
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/digitalcoupon/values.yaml
        - helm/digitalcoupon/overrides/values-apac-prod.yaml
    alert:
      channel: bsp-apac-prd-flux-applications
