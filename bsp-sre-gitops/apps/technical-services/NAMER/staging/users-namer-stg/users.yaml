apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: users-on-boarding
  namespace: users-namer-stg
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: users-namer-stg
      interval: 5m
## Values overridden for this deployment
  values:
    namespace: users-namer-stg
    releaseName: users
    applicationName: users
    gitRepo:
      repoName: core-config
      ref:
        branch: users-namer-stg
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/users/values.yaml
        - helm/users/overrides/namer-stg.yaml
    alert:
      channel: core-gitops
