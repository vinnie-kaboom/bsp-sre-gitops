apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apims-auditing-onboarding
  namespace: stg-apims
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: stg-apims
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: stg-apims
    releaseName: auditing-staging-ts-rextvze
    applicationName: auditing
    gitRepo:
      repoName: apims-auditing-config
      ref:
        branch: namer-stg
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp          
    helmRelease:
      valuesFiles:
        - helm/auditing/values.yaml
        - helm/auditing/overrides/namer-staging.yaml
    alert:
      channel: apims-auditing-gitops