apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: core-on-boarding
  namespace: core-namer-sandbox
spec:
  interval: 60m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: core-namer-sandbox
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: core-namer-sandbox
    releaseName: core
    applicationName: nep-core
    gitRepo:
      repoName: nep-core-config
      ref:
        branch: namer-sandbox
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      interval: 60m
      timeout: 55m # Needed to give other jobs time to finish.
      valuesFiles:
        - helm/nep-core/values.yaml
        - helm/nep-core/overrides/namer-sandbox.yaml
    alert:
      channel: core-gitops