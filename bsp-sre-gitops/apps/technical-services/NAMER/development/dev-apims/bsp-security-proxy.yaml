apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: bsp-security-proxy-onboarding
  namespace: dev-apims
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: dev-apims
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: dev-apims
    releaseName: bsp-security-proxy-bmy3hvz
    applicationName: bsp-security-proxy
    gitRepo:
      repoName: bsp-security-proxy-config
      ref:
        branch: namer-dev-transition
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/bsp-security-proxy/values.yaml
        - helm/bsp-security-proxy/overrides/namer/namer-dev-override.yaml
    alert:
      channel: shared-flow-gitops