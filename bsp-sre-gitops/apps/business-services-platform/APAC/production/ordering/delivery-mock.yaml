apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: delivery-mock-on-boarding
  namespace: ordering
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: ordering
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: ordering
    releaseName: delivery-mock-apac-prd
    applicationName: delivery-mock
    gitRepo:
      repoName: delivery-mock-config
      ref:
        branch: apac-prd
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/delivery-mock/values.yaml
        - helm/delivery-mock/overrides/apac-prd.yaml
    alert:
      channel:
        - bsp-apac-prd-flux-applications
        - hoth-flux-prd-apac
