apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: delivery-expiration-processor-on-boarding
  namespace: ordering
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: ordering
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: ordering
    releaseName: delivery-expiration-processor-apac-prd
    applicationName: delivery-expiration-processor
    gitRepo:
      repoName: delivery-expiration-processor-config
      ref:
        branch: apac-prd
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      valuesFiles:
        - helm/delivery-expiration-processor/values.yaml
        - helm/delivery-expiration-processor/overrides/apac-prd.yaml
    alert:
      channel:
        - bsp-apac-prd-flux-applications
        - hoth-flux-prd-apac
