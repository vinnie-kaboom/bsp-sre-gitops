apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: core-on-boarding
  namespace: prd-core
spec:
  interval: 60m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: ghe-on-board-helm
        namespace: prd-core
      interval: 5m
  ## Values overridden for this deployment
  values:
    namespace: prd-core
    releaseName: core
    applicationName: nep-core
    gitRepo:
      repoName: nep-core-config
      ref:
        branch: emea-prd
      secretRef:
        name: ghe-repository-auth
      orgName: ncrvoyix-bsp
    helmRelease:
      timeout: 55m # Needed to give other jobs time to finish.
      interval: 60m
      valuesFiles:
        - helm/nep-core/values.yaml
        - helm/nep-core/overrides/emea-prd.yaml
    alert:
      channel: core-gitops
