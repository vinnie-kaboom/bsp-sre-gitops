apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apims-auditing-onboarding
  namespace: dev-apims
spec:
  interval: 5m
  chart:
    spec:
      chart: ./helm/bsp-applications
      sourceRef:
        kind: GitRepository
        name: on-board-helm
        namespace: dev-apims
      interval: 5m
  values:
    namespace: dev-apims
    releaseName: auditing
    applicationName: auditing
    gitRepo:
      repoName: apims-auditing-config
      ref:
        branch: namer-dev
    helmRelease:
      valuesFiles:
        - helm/auditing/values.yaml
        - helm/auditing/overrides/asm/namer-dev.yaml
    alert:
      channel: apims-auditing-gitops